<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monte.ondb">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >

        </activity>
        <activity android:name=".Chooseuser" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".Welcome" />
        <activity android:name=".Sign" >

        </activity>
        <activity android:name=".Congratulation" />
        <activity android:name=".Admin" />
        <activity android:name=".Supervisor" />
        <activity android:name=".Agency" />
        <activity android:name=".Superlog" />
        <activity android:name=".Agencylog" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Adminfirst">

        </activity>
        <activity android:name=".Choose_Country" />
        <activity android:name=".Document" />
        <activity android:name=".chat" />
        <activity android:name=".Booking" >

        </activity>
        <activity android:name=".Edit_book" />
        <activity android:name=".super_profile" />
        <activity android:name=".service" />
        <activity android:name=".Request" />
        <activity android:name=".Agency_prof" />
        <activity android:name=".Add_adv" />
        <activity android:name=".Update_adv" />
        <activity android:name=".Show_adv" />
        <activity android:name=".Rate" />
        <activity
            android:name=".Edit_profile"
            >

        </activity>
        <activity android:name=".Show_rate" />
        <activity android:name=".My_document" />
        <activity android:name=".Image" />
        <activity android:name=".Confirm" />
        <activity android:name=".Introduce"></activity>
        <activity android:name=".Distribution">

        </activity>
    </application>

</manifest>